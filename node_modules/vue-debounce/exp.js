function compatibility (listener) {
  if (listener.startsWith('on')) {
    return listener.split('').slice(2).join('')
  }

  return listener
}

console.log(compatibility('onclick'))

console.log(compatibility('click'))
